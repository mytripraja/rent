# V6.6 Build Fix

Fixed the Vercel production build failure caused by importing `Stairs` from `lucide-react` while the project's locked `lucide-react` version is 0.383.0 and does not export that icon.

The blueprint feature still displays **Stairs** as the feature label, but uses the compatible `Milestone` Lucide icon internally.

No other dependency versions were changed.
