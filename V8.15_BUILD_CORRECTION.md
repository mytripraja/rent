# V8.15 Build Correction

## Fixed
- Corrected `src/modules/shared/UserSettingsModal.jsx` conditional section JSX.
- Removed stray closing `</section>` tags that caused Vercel/esbuild to report `Unterminated regular expression` at the following `</section>}` line.
- Normalized every settings-tab conditional to the form `{condition && <section>...</section>}`.
- Bumped app version to 0.1.15 in package.json and package-lock.json.

## Verification
- Inspected the exact V8.14 source that produced the Vercel error.
- Confirmed the offending extra closing tags are removed.
- ZIP integrity checked after packaging.
- Full production Vite build could not be run locally because dependency installation timed out in the working environment.
