# V8.11 — Mobile Tenant Rent UX & Resilience

## Included
- Redesigned tenant rent hero with clearer monthly status, stronger mobile tap target, and loading skeleton.
- Rent history now resolves the current house once and uses the actual house rent amount for progress/receipts instead of relying only on stale auth context.
- Receipt house details now use the real internal door number.
- Mobile Rent screen gets a short usage hint and tighter spacing.
- Added an explicit `/favicon.ico` link while keeping the PNG fallback.

## Validation
- Source edits completed against V8.10.
- ZIP integrity and syntax checks should be run before deployment.
- Full Vite build is not claimed here because dependencies are not installed in this working container.
