# V8.23 Build Fix

Fixed a duplicate `sendingLogin` / `setSendingLogin` state declaration in `src/modules/owner/TenantProfile.jsx` introduced in the tenant login setup update. The duplicate declaration caused Vite/esbuild to fail with `The symbol "sendingLogin" has already been declared`.

Version: 0.1.23
