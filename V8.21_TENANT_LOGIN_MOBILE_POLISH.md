# V8.21 — Tenant login setup & mobile header polish

- Mobile owner header no longer reserves space for the owner name, preventing apartment switcher/control overlap on narrow screens.
- Property switcher has tighter mobile bounds and safer dropdown width.
- Added owner-level “Send login setup” action to Tenant Profile when an email is linked.
- The server generates a Firebase password-reset/setup link and sends it through the configured email provider; no password is stored or exposed.
- Added an audit record for login-setup emails.
- Existing optional tenant email flow remains unchanged.
